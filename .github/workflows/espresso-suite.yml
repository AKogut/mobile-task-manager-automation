name: Espresso suite (reusable)

on:
  workflow_call:

permissions:
  contents: read

jobs:
  espresso:
    name: Espresso (Android Emulator)
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: app/package-lock.json

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version-file: .java-version

      - name: Set up Android SDK
        uses: android-actions/setup-android@v4

      - name: Install Android SDK packages
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0" "system-images;android-35;google_apis;x86_64"

      - name: Install app dependencies
        working-directory: app
        run: npm ci

      - name: Verify generated test ids are current
        run: |
          npm run android:test:testids
          if ! git diff --quiet -- android-tests/src/androidTest/kotlin/com/mobiletaskmanager/TestIds.kt; then
            echo 'TestIds.kt is out of date. Run "npm run android:test:testids" and commit the result.' >&2
            git --no-pager diff -- android-tests/src/androidTest/kotlin/com/mobiletaskmanager/TestIds.kt
            exit 1
          fi

      - name: Create Espresso runner script
        run: |
          cat > /tmp/run-espresso-tests.sh << 'EOF'
          #!/usr/bin/env bash
          set -eo pipefail

          echo "Waiting for the package manager..."
          DEADLINE=$(( $(date +%s) + 120 ))
          until adb shell pm list packages 2>/dev/null | grep -q "^package:"; do
            if [ "$(date +%s)" -ge "$DEADLINE" ]; then
              echo "Package manager still not ready after 120s — continuing anyway"
              break
            fi
            sleep 3
          done

          adb shell input keyevent KEYCODE_WAKEUP 2>/dev/null || true
          adb shell wm dismiss-keyguard 2>/dev/null || true

          adb shell wm size 1280x2856 2>/dev/null || true
          adb shell wm density 480 2>/dev/null || true
          echo "Screen: $(adb shell wm size | tail -1) / $(adb shell wm density | tail -1)"

          cd "$GITHUB_WORKSPACE/app/android"
          ./gradlew :app:connectedAndroidTest -PreactNativeArchitectures=x86_64 --console=plain
          EOF
          chmod +x /tmp/run-espresso-tests.sh

      - name: Run Espresso suite on Android emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 35
          target: google_apis
          arch: x86_64
          profile: Nexus 6
          avd-name: Nexus6_API_35
          emulator-boot-timeout: 900
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -memory 4096
          disable-animations: true
          script: bash /tmp/run-espresso-tests.sh

      - name: Upload HTML report
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v5
        with:
          name: espresso-report
          path: app/android/app/build/reports/androidTests/connected/uitest
          retention-days: 7

      - name: Upload JUnit results and logcat
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v5
        with:
          name: espresso-results
          path: app/android/app/build/outputs/androidTest-results/connected/uitest
          retention-days: 7
