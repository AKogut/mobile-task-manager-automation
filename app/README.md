# Task Manager App

React Native 0.85 application (TypeScript, strict mode).

Documentation, setup, and scripts are in the [repository root README](../README.md).

```bash
npm install
npm run ios      # requires CocoaPods: cd ios && bundle exec pod install
npm run android
npm run start
```
